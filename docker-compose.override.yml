services:
  api:
    environment:
      ASPNETCORE_ENVIRONMENT: Development

  nginx:
    volumes:
      - ./nginx/conf.d/default.local.conf:/etc/nginx/conf.d/default.conf:ro

  db:
    ports:
      - "5432:5432"